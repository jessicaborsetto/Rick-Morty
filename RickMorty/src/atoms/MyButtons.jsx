import { Button } from "@material-tailwind/react";

function MyButtons() {
    return (
        <>
        <Button className="pageBtn rounded-full bg-zinc-600">Character</Button>
        <Button className="pageBtn rounded-full bg-zinc-600">Locations</Button>
        </>
    )
}

export default MyButtons